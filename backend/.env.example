# Database Configuration
# Copy this file to .env and update the values as needed

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=040601
DB_NAME=trackp

# Server Configuration
PORT=8080

# Note: These are the default values used in the application.
# You can set these as environment variables or modify the defaults in main.go
